I am trying to include a number of each event as shown in the html below. The number is being precede by a 
colon that is determined by the css. how do I exclude the colon preceding the number?

<section class="mt-5">
    <div class="row d-flex justify-content-center">
      <div class="col-12 col-md-12 d-flex flex-column align-items-center">
        <div class="row d-flex justify-content-center">
          <div class="col">
            <div class="table-responsive">
              <div class="table-section">
                <div class="table-section">
                  <h5 class="section-title">Events</h5>

                  <table class="table table-sm responsive-stack always-card">
                    <colgroup>
                      <col class="col-event" />
                      <col class="col-date" />
                      <col class="col-time" />
                      <col class="col-location" />
                      <col class="col-address" />
                      <col class="col-city" />
                      <col class="col-state" />
                    </colgroup>

                    <thead>
                      <tr style="font-size: 16px">
                        <th>Event</th>
                        <th>Date</th>
                        <th>Time</th>
                        <th>Location</th>
                        <th>Address</th>
                        <th>City</th>
                        <th>State</th>
                      </tr>
                    </thead>

                    <tbody>
                      @for (event of notice?.events; track $index) {
                      <tr style="font-size: 16px">
                        <td>{{ $index + 1 }}</td>
                        <td
                          data-label="Event"
                          style="text-transform: capitalize"
                        >
                          {{ event.type }}
                        </td>
                        <td data-label="Date">
                          {{
                            formatDateForInput(event.date) | date : "MM/dd/yyyy"
                          }}
                        </td>
                        <td data-label="Time">
                          {{ event.time ? (event.time | toAmPmPipe) : "" }}
                        </td>
                        <td data-label="Location">
                          {{ event.location }}
                        </td>
                        <td data-label="Address">
                          {{ event.address }}
                        </td>
                        <td data-label="City">
                          {{ event.city }}
                        </td>
                        <td data-label="State">
                          {{ event.state }}
                        </td>
                      </tr>
                      }
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

relevant css:

/* ---------- ALWAYS-CARD: card display on all sizes, but 2-per-row on md+ ---------- */

/* make sure wrapper controls overall width (keeps heading aligned) */
.table-section {
  max-width: 900px;
  margin: 0 auto;
}

/* base table style (acts as a block container for card grid) */
.always-card {
  width: 100%;
  border-collapse: collapse;
  table-layout: auto; /* not using fixed layout because it's card-based */
}

/* hide the thead because cards will show labels via data-label */
.always-card thead {
  display: none;
}

/* tbody becomes the grid container that lays out rows as cards */
.always-card tbody {
  display: grid;
  grid-template-columns: 1fr; /* mobile: single column */
  gap: 1rem;                  /* space between cards */
  padding: 0;
  margin: 0;
  list-style: none;
}

/* each table row becomes a card (grid item) */
.always-card tr {
  display: block;
  margin: 0;
  border: 2px solid #ddd;
  border-radius: .25rem;
  padding: .6rem;
  background: #fff;
  box-sizing: border-box;
}

/* stack each cell inside the card */
.always-card td {
  display: block;
  width: 100%;
  padding: .25rem .5rem;
  box-sizing: border-box;
  border-bottom: 0;
  white-space: normal;
}

/* show the label from data-label before each cell's content */
.always-card td::before {
  content: attr(data-label) ": ";
  font-weight: 600;
  display: inline-block;
  width: 120px;   /* label column width inside the card (adjust as needed) */
  vertical-align: top;
}

/* optional: reduce label width on very small screens so text wraps nicely */
@media (max-width: 360px) {
  .always-card td::before { width: 90px; }
}

/* ---------- Medium+ screens: two cards per row ---------- */
@media (min-width: 768px) {

  /* increase spacing and padding on larger screens */
  .always-card tr {
    padding: .75rem;
  }

  .always-card td {
    padding: .35rem .6rem;
    font-size: 15px;
  }

  /* optional: slightly wider label column on larger screens */
  .always-card td::before {
    width: 80px;
  }
}


/* ---------- Large screens: optional tweaks ---------- */
@media (min-width: 1200px) {
  .always-card tbody {
    gap: 1.25rem;
  }
  .always-card td::before { width: 160px; }
}



@media (min-width: 992px) {
    #announcement {
      width: 70% !important;
    }
  }

  textarea:focus {
    outline: none;
}




@media (min-width: 768px) {
.always-card tbody > tr {
    width: 100%;
    max-width: 420px;
  }
}


@media (min-width: 768px) {
  .always-card tbody {
    grid-template-columns: repeat(auto-fit, minmax(420px, 1fr));
    justify-content: center;
    align-items: start;
  }
}